import React, { useState, } from 'react';

import "./Search.scss"

export default function Search({search, results}) {
    return (
        <div className="search">
            Search some thing
        </div>
    )
}